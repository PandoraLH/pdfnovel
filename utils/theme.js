import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
   palette: {
      primary: {
         main: "rgb(29 78 216 / var(--tw-text-opacity))",
      },
      background: {
         main: "#e2e8f0",
      },
      secondary: {
         main: "rgb(103 232 249 / var(--tw-bg-opacity))",
      },
      searchbar: {
         main: "#f3b03d",
      },
   },
});
